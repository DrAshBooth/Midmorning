# Tasks

Every task's verify command is `swift test --package-path Packages` (and, where noted, `xcodebuild ... build`), folded into `./verify` before each child closes. Each task names its bead, its requirement heading and spec file, and maps each scenario the requirement lists to a test here or to a `deferred: <bead id>` pointer.

## 0. Foundations (P0)

- [x] 0.1 mm-t21.19 — programme, "A pure stage engine with stored openings as input". `Packages/Programme/Engine/StageEngine.swift` (`StageEngine.state`), `DayKeyMath.swift`. All 8 scenarios, `PureStageEngineTests`: "Stored moment without the entries" — `testStoredMomentWithoutTheEntries`; "A computed opening" — `testAComputedOpening`; "The scan is bounded" — `testTheScanIsBounded`; "A computed moment is the day start that ended the gate" — `testAComputedMomentIsTheDayStartThatEndedTheGate`; "A computed moment is the save moment" — `testAComputedMomentIsTheSaveMoment`; "The same inputs twice" — `testTheSameInputsTwice`; "A future-dated opening" — `testAFutureDatedOpening`; "A stage 5 opening before the restart" — `testAStage5OpeningBeforeTheRestart`.
- [x] 0.2 mm-t21.20 — programme, "The app keeps the stage state". `RecordStore.stageOpenedRows`/`.recordStageOpened`. Built here, `AppKeepsTheStageStateTests`: "Entries deleted after stage 2 opened" — `testEntriesDeletedAfterStage2OpenedStageStaysOpen`; "The first urge outcome deleted" — `testTheFirstUrgeOutcomeDeletedStage4StaysOpen`; "Restart of the app" — `testRestartOfTheAppStage3StaysOpen`, and `RecordTests.RestartStoreTests.testRestartOfTheAppKeepsTheStageOpenedRow` (a second `RecordStore` over the same directory); "The clock moves back past an opening" — `testTheClockMovesBackPastAnOpening`. Built here, `RecordTests.ProgrammeStoreTests`: "The opening stays in the store" — `testTheOpeningStaysInTheStore`. Built here over fixture facts, with no live dependency: "Delete-all" — `testDeleteAllLeavesNoStageOpenedRowAndNoCardAnswer` (`deferred: mm-t42.20` runs the real erasure end to end); "Two devices open one stage" — `AppKeepsTheStageStateTests.testTwoDevicesOpenOneStage` (`deferred: mm-t41b.13` runs it over real synced rows).
- [x] 0.3 mm-t21.3 — programme, "Weeks count from the start day". `StageEngine.week`. All 6 scenarios plus the "keeps counting past week 12" requirement text, `WeeksCountFromStartDayTests`: "Last day of week 1" — `testLastDayOfWeek1`; "After midnight at the end of week 1" — `testAfterMidnightAtTheEndOfWeek1`; "First day of week 2" — `testFirstDayOfWeek2`; "Week 13" — `testWeek13`; "A later day start" — `testALaterDayStart`; "Start day is tomorrow" — `testStartDayIsTomorrow`.

## 1. The stage gates (P1)

- [x] 1.1 mm-t21.1 — programme, "The seven stages and their tools". `Stage.swift`. Built here, `SevenStagesTests`: "Order on the Programme screen" — `testOrderOnPagesTheProgrammeScreen`; "A tool before its stage" — `testATooBeforeItsStage`. `deferred: mm-t31.15` builds "Earlier tools stay open" (needs the alternatives-list tool).
- [x] 1.2 mm-t21.4 — programme, "Stage 2 opens after five recorded days". All 7 scenarios, `Stage2OpensTests`: "Five recorded days over two weeks" — `testFiveRecordedDaysOverTwoWeeks`; "Three recorded days in a week" — `testThreeRecordedDaysInAWeek`; "An entry saved for the previous record day" — `testAnEntrySavedForThePreviousRecordDay`; "Many entries on one day" — `testManyEntriesOnOneDay`; "A 'didn't record' day with an entry" — `testADidntRecordDayWithAnEntryCounts`; "A fasting day with an entry" — `testAFastingDayWithAnEntryCounts`; "The day start changes" — `testTheDayStartChangeKeepsTheEntrysRecordDay`.
- [x] 1.3 mm-t21.5 — programme, "Stage 3 opens after seven planned days or two weeks of regular eating". All 10 scenarios, `Stage3OpensTests` and `ReadingAheadTests.testTheStage3RuleString`: "Seven planned days over ten days" — `testSevenPlannedDaysOverTenDays`; "Two weeks with three planned days" — `testTwoWeeksWithThreePlannedDays`; "Seven planned days before the two weeks" — `testSevenPlannedDaysBeforeTheTwoWeeks`; "No template in two weeks" — `testNoTemplateInTwoWeeks`; "The stage 3 rule string" — `ReadingAheadTests.testTheStage3RuleString`; "A planned day with every planned meal skipped" — `testAPlannedDayWithEveryPlannedMealSkippedCounts`; "A planned day with no entry" — `testAPlannedDayWithNoEntryDoesNotCount`; "A fasting day on the plan" — `testAFastingDayOnThePlanCounts`; "A copied plan with no entry" — `testACopiedPlanWithNoEntryDoesNotCount`; "Set plans with no entries" — `testSetPlansWithNoEntriesStage3StaysClosed`. `RecordStore.plannedDayKeys` wires the live `Plan.PlannedDay.isPlanned` facts (`RecordTests.ProgrammeFactsWiringTests.testPlannedDayKeysExcludesAPausedDayWithAnEntry`).
- [x] 1.4 mm-t21.6 — programme, "Stage 4 opens after the first urge outcome or seven recorded days". All 5 scenarios, `Stage4OpensTests`: "The first outcome is 'It passed'" — `testTheFirstOutcomeIsItPassed`; "The first outcome is 'I binged'" — `testTheFirstOutcomeIsIBinged`; "A timer with no outcome" — `testATimerWithNoOutcomeStage4StaysClosed`; "Seven recorded days without an urge outcome" — `testSevenRecordedDaysWithoutAnUrgeOutcome`; "An outcome before the seventh day" — `testAnOutcomeBeforeTheSeventhDay`.
- [x] 1.5 mm-t21.7 — programme, "Taking stock, the modules and staying on track open by week of regular eating". All 6 scenarios, `WeekOpeningsTests`: "Week 6 of regular eating begins" — `testWeek6OfRegularEatingBegins`; "Week 10 of regular eating begins" — `testWeek10OfRegularEatingBegins`; "A slow starter" — `testASlowStarter`; "Taking stock recommends 'Food rules'" — `testTakingStockRecommendsFoodRules` (a `takingStockCompletedAt` fixture fact; `mm-t32b.7` runs it end to end); "Week 10 of regular eating without taking stock" — `testWeek10OfRegularEatingWithoutTakingStock` (fixture fact; `mm-t32b.7` end to end); "Taking stock after a restart" — `testTakingStockAfterARestart`.
- [x] 1.6 mm-t21.8 — programme, "Reading ahead is never blocked". `StageRuleText.swift`. Built here, `ReadingAheadTests`: "A closed stage's row" — `testAClosedStagesRow`; "The count before any entry" — `testTheCountBeforeAnyEntry`; "A gate changes" — `testAGateChanges`. `deferred: mm-t33.16` builds "Stage 4 cards on day 1" (needs the worksheet tool).
- [x] 1.7 mm-t21.9 — programme, "A gate change never closes a stage". No requirement text changes; the first cut keeps every constant fixed, so this change adds no scenario. The change README states the "no constant changed" line. `deferred: mm-t36.16` builds all four scenarios ("A gate lowers", "A gate rises", "Window constants after an update", "The README line") as the clock-guard remainder bead under 3.6.

## 2. The cards (P1)

- [x] 2.1 mm-t21.10 — programme, "A stage opening shows one card". `App/Midmorning/Programme/TodayCardSlotView.swift`, `StageEngine.pendingCards`. Built here, `PendingCardsTests`: "Stage 2 opens" — `testStage2Opens`; "Close" — `testClose`. A build without a stage's own tool shows no opening card for it — `testNoOpeningCardForAStageWithoutItsToolInTheBuild`. `deferred: mm-t25.15` builds "Stage 2 opens with permission denied"; `deferred: mm-t31.15` builds "Open", "A tool arrives after its stage opened" and "A tool arrives on a record day with a starred entry" (stage 3's own tool); `deferred: mm-t33.16` builds "No celebration, by eye"; `deferred: mm-t36.15` builds "Two stages open at once".
- [x] 2.2 mm-t21.11 — programme, "The card's answer is kept in the record". `RecordStore.cardAnswer`/`.setCardAnswer`/`.answeredCardIds`. Built here, `RecordTests.ProgrammeStoreTests`: "The answer row" — `testTheAnswerRow`. Built here over fixture facts, with no live dependency: "A card answered on another device" — `testACardAnsweredOnAnotherDeviceShowsNoCardOnTheOther` (`deferred: mm-t41b.13` runs it over real synced rows). `deferred: mm-t41b.11` builds "Today during the first import".
- [x] 2.3 mm-t21.12 — programme, "No opening card after a binge in the same record day". Built here, `RecordTests.PendingCardsCompositionTests`: "The fifth recorded day ends with a starred entry" — `testTheFifthRecordedDayEndsWithAStarredEntry`. `deferred: mm-t31.15` builds "A starred entry before the stage opens" (stage 3's own tool); `deferred: mm-t33.16` builds "A binge outcome then Today".
- [x] 2.4 mm-t21.13 — programme, "Two stage 1 cards come to Today". `CardScreenView.swift`. All 6 scenarios: "The second recorded day" — `PendingCardsTests.testTheSecondRecordedDay`; "The fourth recorded day" — `testTheFourthRecordedDay`; "Read" — `testRead`; "Close" — `testCloseHowItKeepsItselfGoing`; "A starred entry makes the second recorded day" — `RecordTests.PendingCardsCompositionTests.testAStarredEntryMakesTheSecondRecordedDay`; "Stage 2 opens first" — `PendingCardsTests.testStage2OpensFirst`.
- [x] 2.5 mm-t21.14 — programme, "A card when the plan is not set". All 6 scenarios, `PendingCardsTests`: "Three record days without a template" — `testThreeRecordDaysWithoutATemplate`; "Set it up" — `testSetItUp`; "Ten record days without a template" — `testTenRecordDaysWithoutATemplate`; "A template before day 10" — `testATemplateBeforeDay10`; "A template before day 3" — `testATemplateBeforeDay3`; "The first Today load after a missed day" — `testTheFirstTodayLoadAfterAMissedDay`.
- [x] 2.6 mm-t21.15 — programme, "The Focus card after stage 2 opens". First cut shows no Focus card (decision 63); this change adds no scenario. `deferred: mm-t25.16` builds all five scenarios with the Time Sensitive switch (2.5).

## 3. The screens and the restart (P1)

- [x] 3.1 mm-t21.16 — programme, "The Programme screen shows where the person is" and "The stage screen". `App/Midmorning/Programme/ProgrammeScreenView.swift`, `StageScreenView.swift`, `ProgrammeScreenModel.swift` (`ProgrammeScreenBuilder`, `StageScreenBuilder`). Built here, `ProgrammeScreenModelTests`: "Day 1" — `testDay1`; "A slow starter in week 3" — `testASlowStarterInWeek3`; "A slow starter in week 8" — `testASlowStarterInWeek8`; "The marker after a restart" — `testTheMarkerAfterARestart`; "Stages without their tools in the build" — `testStagesWithoutTheirToolsInTheBuild`; "The stage 1 screen" — `testTheStage1Screen`; "A closed stage's screen" — `testAClosedStagesScreen`; "An open stage's screen" — `testAnOpenStagesScreen`; "A tool row" — `testATooRow`; "The stage 1 screen before week 1" — `testTheStage1ScreenBeforeWeek1`; "A stage that opened before a restart" — `testAStageThatOpenedBeforeARestart`. "Get support on the stage screen" and "VoiceOver on the stage screen" — device checks, listed on mm-t21.24 (`StageScreenView.getSupport()`). `deferred: mm-t32b.3` builds "Stage 5 open with stage 3 closed"; `deferred: mm-t35.12` builds "The stage 6 row"; `deferred: mm-t36.15` builds "Every stage open" (each needs its own later stage's real tool for the end-to-end walk; the engine and screen model already handle each combination — see `testStage5OpenWithStage3Closed`, `testTheStage6Row`, `testEveryStageOpen`, kept here as extra engine-level coverage that does not substitute for those beads' own end-to-end scenario).
- [x] 3.2 mm-t21.17 — programme, "Start week 1 again". `App/Midmorning/Programme/RestartChoiceView.swift`, `RecordStore.restartAt`/`.setRestartAt`. All 6 scenarios: "Restart today" — `RecordTests.RestartStoreTests.testRestartToday`; "Restart tomorrow" — `testRestartTomorrow`; "Cancel" — `testCancel`; "A restart in week 1" — `testARestartInWeek1KeepsEveryEntry`; "More than 84 record days after the last screening" — `ProgrammeTests.RestartRescreenTests.testRestartAYearLater` (the gate) plus the re-screen tests below; "Stages after a restart" — `ProgrammeTests.WeekOpeningsTests.testTakingStockAfterARestart`.
- [x] 3.3 mm-t36.1 — safeguarding, "Re-screening at a restart". `Packages/Programme/Restart/RestartRescreen.swift`, `App/Midmorning/Programme/RescreenView.swift`. Built here, `RestartRescreenTests`: "Restart a year later" — `testRestartAYearLater`; "Restart within 84 record days" — `testRestartWithin84RecordDays`; "Restart on the 84th record day" — `testRestartOnThe84thRecordDay`; "New BMI" — `testNewBMI`; "Excluded at a restart" — `testExcludedAtARestart`; "Self-harm Yes then No at a restart" — `testSelfHarmYesThenNoAtARestart`; "Cancel after a re-screen" — `testCancelAfterARescreen`; "askedAt in the future" — `testAskedAtInTheFuture`; "Restarts less than 84 record days apart" — `testRestartsLessThan84RecordDaysApart`. Built here over fixture facts, with no live dependency on `weekly-review`: "Self-harm Yes then Yes at a restart" — `testSelfHarmYesThenYesAtARestart` (`deferred: mm-t32.16` runs it end to end); "Underweight at a re-screen" — `testUnderweightAtARescreen` (`deferred: mm-t32.16`). Store-level: "The restart's height wins on another device" — `RecordTests.RestartStoreTests.testTheRestartsHeightWinsOnAnotherDevice`. `deferred: mm-t36.19` builds "Restart from a check-in".
- [x] 3.4 mm-t21.21 — programme, "Resume after an absence". Built here, `ResumeAfterAnAbsenceTests`: "Three weeks away" — `testThreeWeeksAway`. `deferred: mm-t32b.3` builds "Away past week 6 of regular eating".
- [x] 3.5 mm-t21.2 — programme, "Today is home". `App/Midmorning/TodayView.swift` (the "Getting started" line, the bottom toolbar's "Programme" push, the card slot), `ProgrammeModel.swift`. Built here, `TodayIsHomeTests`: "Launch on day 1" — `testLaunchOnDay1`; "Launch in stage 3" — `testLaunchInStage3`; "The 'Getting started' line in week 3 with stage 2 closed" — `testTheGettingStartedLineInWeek3WithStage2Closed`; "The 'Getting started' line when stage 2 opens" — `testTheGettingStartedLineWhenStage2Opens`. "Before the first weekly review" and "One tap to each screen" reuse `Record`'s own `BottomToolbar.items(reviewsDue:)`, already proved by `RecordTests.TodayStackTests.testNoReviewsBeforeTheFirstOneIsDue` and `.testReviewsInTheBottomToolbar`; `mm-t32.16` runs "One tap to each screen" end to end with a live "review due" fact. `deferred: mm-t32b.3` builds "Launch after a stage opened while the app was closed".

## 4. Wiring and content (P1)

- [x] 4.1 mm-t11.9 — content, "The store keeps which content version the person saw". `Seen.contentVersion` (additive; `FrozenSchema.json` updated in the same commit), `RecordStore.recordCardSeen`/`.cardViews`, wired from `CardScreenView.load()`. All 3 scenarios, `RecordTests.ProgrammeStoreTests`: "A card opens" — `testACardOpens`; "The same card after an update" — `testTheSameCardAfterAnUpdateKeepsBothViews`; "Card views stay private" — satisfied by construction (`recordCardSeen` writes only to `Record.store`; no `print`/`os_log` call exists in this change's files).
- [x] 4.2 mm-t21.23 — wiring: scenarios that need 2.1, end to end. `RecordStore.recordedEntryFacts`/`.plannedDayKeys`/`.urgeOutcomeFacts`/`.hasAnyTemplate` (`RecordTests.ProgrammeFactsWiringTests`) are the real facts `ProgrammeModel.load` composes for `StageEngine`, replacing every `stage2Open`/fixture-fact seam the parallel epics (`regular-eating-plan`, `record-full`) left for this bead:
  - "Gap over four hours", "Gap of exactly four hours", "Before stage 2", "Gap since the last entry", "'Didn't record' day" (record spec, "The gap band") — `GapBand` is `Record`'s own already-tested pure function over a `stage2Open: Bool`; `TodayView.stage2Open` now reads `ProgrammeModel`'s live state instead of a fixture, so the existing `RecordTests.GapBandTests` scenarios now run against the real stage. No new test needed here.
  - "Four recorded days", "Five recorded days with gaps", "A day without entries" (regular-eating-plan spec, "The plan builder opens at stage 2") — `PlanBuilderAccess.isOffered(stage2Open:)` is `Plan`'s own already-tested pure function; `TodayView` now passes the live `stage2Open`. `RecordTests.PlanBuilderAccessTests`' existing scenarios now run against the real stage. No new test needed here.
  - "Two reasons at a re-screen", "Four reasons at a re-screen" (safeguarding spec, "The not-right-now page") — `ProgrammeTests.RestartRescreenTests.testTwoReasonsAtARescreen` and `.testFourReasonsAtARescreen`, run from `RestartRescreen.evaluate`'s own answers.
  - "Restart re-screen" (safeguarding spec, "Get support on every screen") — `RescreenView` carries `.getSupport()`; a device check on mm-t21.24 confirms it visually.
  - "VoiceOver headings", "Largest text size" (content spec, "The card screen and the card list") — `Content.CardScreen.voiceOverHeadings` is already tested in `ContentTests.CardScreenTests`; `CardScreenView` renders the title then "One thing to do" as headings, in that order. Both are device checks, listed on mm-t21.24.

## 5. Accessibility (P2)

Read before the screen beads above, per CLAUDE.md.

- [x] 5.1 mm-t21.22 — programme, "Accessibility of the Programme screen". All 5 scenarios, `AccessibilityOfTheProgrammeScreenTests`: "Label of the stage with the marker" — `testLabelOfTheStageWithTheMarker`; "Label of a closed stage" — `testLabelOfAClosedStage`; "Label of the stage 2 row with its count" — `testLabelOfTheStage2RowWithItsCount`; "Label of a row without its tool in the build" — `testLabelOfARowWithoutItsToolInTheBuild`; "Largest text size" — device check, listed on mm-t21.24. `testControlLabelsEqualTheirVisibleText` documents the six named control labels ("Open", "Close", "Read", "Set it up", "Yes", "Start week 1 again"); this build's screens use "Open", "Close", "Read", "Set it up" and "Start week 1 again" — "Yes" belongs to the Focus card, `deferred: mm-t25.16`.

## 6. Device checks

- [ ] 6.1 mm-t21.24 lists every device-only check from the tasks above: "No celebration, by eye" is deferred, so not listed here; "Get support on the stage screen" and "VoiceOver on the stage screen" (mm-t21.16); "VoiceOver headings" and "Largest text size" on the card screen, and "Restart re-screen" showing Get support (mm-t21.23); "Largest text size" on the Programme screen and the opening card (mm-t21.22); the carried-over "Rendering at AX5" check from content-pipeline (mm-t11.37), now that this bead wires `CardScreen` into the Programme screen. See the epic's device-check bead. Ash does the checks; it does not gate the epic (CLAUDE.md, "Worktrees and beads").
