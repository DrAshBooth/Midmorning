# Tasks

Every task's verify command is `swift test --package-path Packages`, folded into `./verify` before each child closes. Each task names its bead, its requirement heading and spec file, and maps each scenario the requirement lists to a test here or to a `deferred: <bead id>` pointer.

## 0. Foundation

- [x] 0.1 mm-t12b.3 — product-rules spec, "Appearance" (`#Appearance`), decision 92. `App/Midmorning/Appearance.swift` (the shared list style, sheet detent, `RecordField`, `PredictiveTextView`, `RecordOneLineField`, `FlowLayout`) and `App/Midmorning/Assets.xcassets/AccentColor.colorset` (light `1F4E79`, dark `7CB4E6`, Increase Contrast `5078A0`; each contrasts with the row background at 3:1 or more — see the change README). `TodayView` and `NewEntryView` use the shared modifiers. No requirement text changes: this bead builds shared code only. Evidence: `evidence/appearance-today-light.png`, `evidence/appearance-today-dark.png`, `evidence/appearance-today-ax5.png`.
- [x] 0.2 mm-t12b.2 — record spec, MODIFIED "Today's appearance", "Today shows the record day's entries in time order", "Accessibility of the record", "Create an entry" (decision 64). This change's own delta (`specs/record/spec.md`) carries the full modified text, copied from `openspec/specs/record/spec.md` on the branch, with only the changes the record-full delta's Purpose lists. The three named scenarios ("Previous record day with entries", "Entry with an empty What", "Reading order of the new-entry screen") are rewritten there. `openspec validate record-full --type change --strict` passes as part of `./verify`.

## 1. The Today stack

- [x] 1.1 mm-t12.15 — record, "The Today stack". `App/Midmorning/TodayView.swift`, `DaySection.swift`. Scenarios: "Empty day, no cards" — `TodayStackTests.testEmptyDayNoCards` and `evidence/appearance-today-light.png` (an empty current day, before this change's own entries were seeded, shows the shell only); "Card after a starred entry" — `TodayCardSlotTests.testCardAfterAStarredEntryWaitsForTheNextRecordDay`; "Previous day" — proved by `DaySection.load`'s default-collapse rule (`CollapseTests.testDefaultStates`) and shown live in `evidence/rf-05-today-after-save.png` (the previous day collapsed to "2 entries"); "Add an entry on a long day" — the pinned section header is a plain-list section header, pinned by the system under `recordListStyle()`, and "Add an entry" opens the sheet (`evidence/rf-02-new-entry.png` after a tap). Correction, 26 September 2026 (mm-t12b.8): this claim was wrong. The heading and "Add an entry" were a plain row that scrolled away. Now they are the header of the current day's section, and the system pins that header. Built over fixture facts: "Lapse card in the same record day" — `TodayCardSlotTests.testLapseCardShowsInTheSameRecordDayEveryOtherCardWaits`; "Reviews in the bottom toolbar" — `TodayStackTests.testReviewsInTheBottomToolbar`, wired through `BottomToolbar.items(reviewsDue:)` with the fixture `false` until `weekly-review` (3.2) supplies the live fact. `deferred: mm-t31.17` — "Reading order from stage 3". `deferred: mm-t33.16` — "Two cards waiting".

## 2. Where, Context and the new-entry screen

- [x] 2.1 mm-t12.16 — record, "Where chips". `ItemVersion.whereText`, `Packages/Sources/Record/WhereChips.swift` (`WhereChip`, `CustomPlaces`), `RecordStore.touchCustomPlace`/`customPlaces()`, `App/Midmorning/WhereChipsView.swift`. Scenarios: "Save with a fixed chip" — `WhereContextTests.testSaveWithAFixedChip`; "Save with no Where" — `testSaveWithNoWhere`; "Add a custom place" — `testAddACustomPlace`; "Clear a chip" — `testClearAChip`; "Ninth custom place" — `testNinthCustomPlaceDropsTheLeastRecentlyUsed`; "No suggestion from a previous entry" — `testCustomPlacesNeverIncludeWhatOrContext` (the store never reads a previous entry's What or Context into the chip list; the new-entry screen's `@State` also starts empty on every appearance) plus device confirmation on mm-t12b.1; "Keyboard in 'Add a place'" — `deferred: mm-t12b.1` (device check, decision 89).
- [x] 2.2 mm-t12.17 — record, "The Context field". `ItemVersion.context`, `Packages/Sources/Record/NewEntryLogic.swift` (`ContextLabel`). Scenarios: "Star on changes one label" and "Star off" — `WhereContextTests.testStarTogglesOnlyTheContextLabel`; "Starred entry with an empty Context" — `testStarredEntryWithAnEmptyContext`; "Context with white space" — `testContextWithWhiteSpaceIsTrimmed`; "Keyboard in Context" — `deferred: mm-t12b.1` (device check, decision 89).
- [x] 2.3 mm-t12b.4 — record, "The new-entry screen's controls". `NewEntryField.order`, `NewEntryTime.resolve`, `App/Midmorning/NewEntryView.swift`. Scenarios: "Order of the controls" — `WhereContextTests.testOrderOfTheControls`; "Return in What" — `PredictiveTextView`'s `onReturn` is `nil` for What, so Return inserts a line break by construction, confirmed live in `evidence/rf-04-context-filled.png`-adjacent runs; "Keyboard in What" — `deferred: mm-t12b.1`; "Save from the keyboard's toolbar" — `PredictiveTextView.makeAccessoryToolbar()` sets `inputAccessoryView` on every field on this screen, confirmed live (`evidence/rf-04-context-filled.png` shows the floating "Save" control above the keyboard); "The time control in the morning" — `WhereContextTests.testTimeControlInTheMorning`; "Last night's time after midnight" — `testLastNightsTimeAfterMidnight`; "A time before the day start" — `testATimeBeforeTheDayStart`.
- [x] 2.4 mm-t12.18 — record, "Today shows Where and Context". `App/Midmorning/TodayView.swift`'s `EntryRow`. Scenarios: "Entry with Where and Context" — `WhereContextTests.testEntryWithWhereAndContext`; "Entry with an empty What and a Where" — `testEntryWithAnEmptyWhatAndAWhere`; "Long Context at the largest text size" — `EntryRow` uses no `lineLimit`, confirmed live at AX5 in `evidence/appearance-today-ax5.png` (no truncation of the visible rows).

## 3. Edit and delete

- [x] 3.1 mm-t12.19 — record, "Edit an entry". `RecordStore.update`, `App/Midmorning/EditEntryView.swift`. Scenarios: "Change the What" — `EditDeleteTests.testChangeTheWhat`; "Change the time inside the entry's record day" — `testChangeTheTimeInsideTheEntrysRecordDay`; "One segment on the edit screen" — `testEditTimeControlBoundsComeFromTheEntrysOwnRecordDay` and live in `evidence/rf-06-edit-screen.png`; "Creation moment stays" — `testCreationMomentStays`; "Cancel an edit" — `testCancelAnEditCallsNoUpdate`.
- [x] 3.2 mm-t12.20 — record, "Delete an entry". `RecordStore.delete`, the edit screen's "Delete entry" and Today's swipe action. Scenarios: "Delete from the edit screen" — `EditDeleteTests.testDeleteFromTheEditScreen` and live in `evidence/rf-07-delete-confirm.png` / `rf-08-today-after-delete.png`; "Cancel a delete" — `testCancelADeleteCallsNoDelete`; "Delete the only entry of the previous record day" — `testDeleteTheOnlyEntryOfThePreviousRecordDay`; "Deleted entry after restart" — `testDeletedEntryStaysHiddenAfterRestart`.
- [x] 3.3 mm-t12.27 — record, "A save that fails". `Packages/Sources/Record/SaveOutcome.swift`. Scenario: "Full storage" — `EditDeleteTests.testFailureMessageText` proves the exact text; the screen keeping the typed text and the chosen values holds by construction, because `NewEntryView.save()` and `EditEntryView.save()` only set `saveOutcome = .failed` in the `catch` branch and touch no other `@State`.

## 4. Day states

- [x] 4.1 mm-t12.21 — record, "'Didn't record'". `RecordStore.dayStates`/`setDayState`, the day heading's `Toggle`. Scenarios: "Set the current day" and "Set an earlier day" — `DayStatesTests.testSetTheStateOnTheCurrentDayAndOnAnEarlierDay`; "Turn it off" — `testTurnItOff`; "Entry on a 'didn't record' day" — `testEntryOnADidntRecordDayKeepsBoth`.
- [x] 4.2 mm-t12.22 — record, "'Pause for today'". `App/Midmorning/TodayView.swift`'s pause control. Scenarios: "Pause" and "Undo a pause" — `DayStatesTests.testPauseAndUndoAPause` and live in `evidence/rf-05-today-after-save.png`/`rf-01-today.png` (the control's two states); "Entry on a paused day" — `testEntryOnAPausedDayStillSaves`; "The next day" and "Paused day seen later" — `testPauseIsKeyedPerRecordDay`.
- [x] 4.3 mm-t12.23 — record, "'Fasting today'". Scenarios: "Turn it on" and "The day ends" — `DayStatesTests.testFastingIsKeyedPerRecordDay`; "Entries on a fasting day" — `testFastingDayIsAGapBandExemptState`.
- [x] 4.4 mm-t12.28 — record, "The app keeps day states on the device". Scenarios: "Restart" — `DayStatesTests.testStateSurvivesReopeningTheStore`; "No network" — `testNoNetworkNeededToPauseOrCollapse` (every call is a local `RecordStore` write); "A failed state save" — `testAFailedStateSaveThrowsWithNoData`.

## 5. Collapse, gap band and earlier days

- [x] 5.1 mm-t12.25 — record, "Collapse a day to a count". `Packages/Sources/Record/CollapseDefault.swift`, `RecordStore.collapseChoice`/`setCollapseChoice`. Scenarios: "Collapse the current day" and "One entry" — `CollapseTests.testCollapseReadsTheStoresEntryCount`; "Expand" and "Save into a collapsed day" — `testExpandAndSaveIntoACollapsedDayBothKeepExpanded`; "Empty day" — `testEmptyDayHasNoEntries`; "Default states" — `testDefaultStates`; "Save into the previous day" and "Kept choice after the app opens again" — `testKeptChoiceSurvivesReopeningTheStore`; "Default state of an earlier day" — `testDefaultStateOfAnEarlierDay`.
- [x] 5.2 mm-t12.26 — record, "The gap band". `Packages/Sources/Record/GapBand.swift`, `App/Midmorning/TodayView.swift`'s `GapBandRow`. Built over fixture facts (`stage2Open`), `mm-t21.23` wires the live stage. Scenarios: "Gap over four hours" — `GapBandTests.testGapOverFourHoursShowsABand`; "Gap of exactly four hours" — `testGapOfExactlyFourHoursShowsNoBand`; "Before stage 2" — `testBeforeStage2ShowsNoBand`; "Gap since the last entry" — `testGapSinceTheLastEntryShowsNoBandAfterIt`; "'Didn't record' day" — `testDidntRecordDayShowsNoBand`.
- [x] 5.3 mm-t12.24 — record, "Earlier record days". `Packages/Sources/Record/EarlierDays.swift`, `RecordStore.dateKeysWithContent`, `App/Midmorning/EarlierDaysListView.swift`. Scenarios: "Open an earlier day" — `EarlierDaysTests.testOpenAnEarlierDay`; "The list shows dates only" — `testTheListShowsDatesOnly`; "Move between days" and "Return to Today" — `testMoveBetweenDaysAndReturnToToday`; "No earlier day yet" — `testNoEarlierDayYet`; "An earlier day with a state only" — `testAnEarlierDayWithAStateOnly`.

## 6. Accessibility of the additions

- [x] 6.1 mm-t12.29 — record, "Accessibility of the additions". Scenarios: "Label of a full row" — `WhereContextTests.testLabelOfAFullRow`; "Label of a row with Where only" — `testLabelOfARowWithWhereOnly`; "The band" — `GapBandTests.testAccessibilityLabelFillsTheConstant`; "Reading order with Where and Context", "Reading order on the edit screen" and "Delete with VoiceOver" — proved by construction (`NewEntryField.order` drives both screens' layout, `EditEntryView` appends "Delete entry" last, and `TodayView`'s row carries a VoiceOver "Delete" custom action) and named on the epic's device-check bead (mm-t12b.1) for the on-device VoiceOver walk.

## 7. Device checks

- [x] 7.1 mm-t12b.1 lists every device-only check from the tasks above (decision 89's inline-prediction checks, VoiceOver focus and reading order, the largest-text large content viewer, and the shame walk). See the epic's device-check bead.
