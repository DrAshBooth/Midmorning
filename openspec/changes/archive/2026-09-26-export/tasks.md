# Tasks

Every task's verify command is `swift test --package-path Packages` (and, where noted, `xcodebuild ... build`), folded into `./verify` before each child closes. Each task names its bead, its requirement heading and spec file, and maps each scenario the requirement lists to a test here or to a `deferred: <bead id>` pointer.

## 0. Foundations (P0)

- [x] 0.1 mm-t42.10 — export, "The document and the paginator live in a package". `Packages/Sources/Export/ExportDocument.swift`, `Paginator.swift`, `ExportFileName.swift`. Both scenarios, `ExportTests.PaginatorTests`: "Pagination with a stub measurer" — `testALongDaySplitsAcrossPagesAndRepeatsItsHeadingOnTheContinuation`; "No UIKit in the package" — `testThePackageSourceImportsNoUIKit` (a source-level grep; the target also only compiles under `swift test` on macOS, where UIKit does not exist, at all).

## 1. The screen and the document (P1)

- [x] 1.1 mm-t42.1 — export, "Choose a date range". `Packages/Sources/Export/ExportRange.swift`, `ExportContent.swift`; `App/Midmorning/Export/ExportScreenView.swift`. All 6 scenarios: "Default range" — `ExportRangeTests.testDefaultRangeIsTwentySevenDaysBack`; "Short record" — `testDefaultRangeStartsAtTheEarliestEntryWhenLater`; "A range of one day" — `ExportDocumentBuilderTests.testARangeOfOneDayHoldsThatDayAndNoOther`; "To before From" — `ExportRangeTests.testFromIsNotAllowedAfterTo` (the screen's own `DatePicker(in:)` upper bound is the same rule, App target, untested directly); "The switches when the screen opens" — `testTheSwitchDefaults`; "The line above 'Make PDF'" — `testTheDisclosureLine`.
- [x] 1.2 mm-t42.2 — export, "The PDF is formatted like the paper record". `Packages/Sources/Export/ExportDocumentBuilder.swift`, `ExportDayKey.swift`; `App/Midmorning/Export/ExportPDFRenderer.swift`. All 9 scenarios, `ExportTests.ExportDocumentBuilderTests` unless noted: "The first page" — `testFirstPageOrderOfFrontMatter`; "Two days in order" — `testTwoDaysInOrder`; "Entry after midnight" — `testEntryAfterMidnightStaysUnderItsStoredDayKey`; "A deleted entry" — `testADeletedEntryNeverReachesTheDocument` (over a real `RecordStore`); "The day start changed after a save" — `testChangingTheDayStartNeverMovesAStoredEntry`; "A day start other than 04:00" — `testADayStartOtherThan0400`; "Days flow on one page" — `PaginatorTests.testThreeDaysWithTwoEntriesEachFlowOnOnePage`; "A long day over two pages" — `PaginatorTests.testALongDaySplitsAcrossPagesAndRepeatsItsHeadingOnTheContinuation`; "Context left out" — `testContextLeftOutHasNoContextHeadingOrText`.
- [x] 1.3 mm-t42.3 — export, "Each entry in the PDF". `ExportDocumentBuilder.swift` (the `RecordRow` → `ExportEntryLine` map). All 3 scenarios, `ExportDocumentBuilderTests`: "A starred entry" — `testAStarredEntry`; "An unstarred entry with What only" — `testAnUnstarredEntryWithWhatOnly`; "An entry saved the next morning" — `testAnEntrySavedTheNextMorningShowsNoCreationLabel`.
- [x] 1.4 mm-t42.4 — export, "Days with no entries, 'didn't record' days and paused days". `ExportDocumentBuilder.swift` (state lines). All 3 scenarios, `ExportDocumentBuilderTests`: "Empty day" — `testEmptyDayShowsHeadingAndNothingElse`; "'Didn't record' day" — `testDidntRecordDay`; "Paused day with entries" — `testPausedDayWithEntriesShowsStateThenEntries`. Also `testBothStatesShowDidntRecordFirst` (both states together, the requirement's own ordering rule).
- [x] 1.5 mm-t42.5 — export, "The optional weigh-in page". `ExportWeighInPageBuilder.swift` (reuses `Programme.WeighInWeight.display`). All 4 scenarios, `ExportWeighInPageBuilderTests`/`ExportWeighInPageTests`: "Weigh-ins included" — `testWeighInsIncludedListsFiveRowsInKg`; "Weigh-ins in stone and pounds" — `testWeighInsInStoneAndPounds`; "Weigh-ins not included" — `ExportWeighInPageTests.testNoWeighInLinesOmitsThePage` (an empty line list omits the page, at the caller); "No weigh-in in the range" — `testRowsOutsideTheRangeAreExcluded`.
- [x] 1.6 mm-t22.13 — weigh-in, "The weigh-in page in an export". `ExportWeighInPageBuilder.swift`. All 3 scenarios, `ExportWeighInPageBuilderTests`: "Weigh-ins included" — `testWeighInsIncludedListsFiveRowsInKg`; "Weigh-ins not included" — `ExportWeighInPageTests.testNoWeighInLinesOmitsThePage`; "Weigh-ins after an opt-out" (decision 103) — `testWeighInsAfterAnOptOutStillListsEveryKeptRow` (the builder never reads the weigh-in day at all).
- [x] 1.7 mm-t42.12 — settings, "The Weigh-in group" (the export half of "Change the unit"). `App/Midmorning/Export/ExportComposer.swift` reads `store.weighInUnit()`. The one scenario, `ExportWeighInPageBuilderTests.testChangingTheUnitChangesDisplayOnly`.
- [x] 1.8 mm-t42.7 — export, "Share sheet only". `App/Midmorning/Export/ExportComposer.swift` (the protected temporary file), `ExportScreenView.swift` (deletes it on the share sheet's dismissal, cancel or send alike), `ShareSheetView.swift`. All 4 scenarios: "Share" and "Cancel the share sheet" and "After a share" — the one `onDismiss` cleanup path covers all three (App target, untested directly by `swift test`; `ExportComposer.writeTemporaryFile`/`.deleteTemporaryFile` are the pure file operations); "No password" — `ExportPDFRenderer` sets no `kCGPDFContextOwnerPassword`/`UserPassword` key, structural (no such call exists in the source).
- [x] 1.9 mm-t42.6 — export, "What the PDF never contains". `ExportPDFRenderer.swift` (Creator empty, no Author, Producer left to the system), `ExportFileName.swift`. All 4 scenarios: "A day with fifteen entries" — `ExportDocumentBuilderTests.testFifteenEntriesShowNoCount` (the value types carry no count field at all); "A gap in the range" — structural, the builder computes no gap band; "File name" — `ExportFileNameTests.testFileName`; "Metadata" — `testPDFTitle`, `testFileNameHasNoProductNameOrPersonName`.
- [x] 1.10 mm-t42.8 — export, "The export offered by the deterioration rule". `App/Midmorning/Safeguarding/ExportControlButton.swift`, pushed from `GPSuggestionPageView.swift` (the deterioration rule's own reason). Both scenarios: "Opened from the deterioration rule" and "Change the range" — the export screen never shows text about why it opened and always starts from the default range, regardless of caller (App target composition; `ExportScreenView` takes no "reason" parameter at all, so there is nothing for a caller to pass).
- [x] 1.11 mm-t42.9 — export, "Offline and out of logs". `ExportScreenView.makePDF` (App target). Both scenarios: "Airplane mode" — structural, the export path makes no network call; "Build error" — `ExportRangeTests.testTheBuildErrorMessage` (the fixed, data-free message the catch site always shows).
- [x] 1.12 mm-t42.11 — export, "Accessibility of the export". Proves what 1.1–1.9 already built (CLAUDE.md: the P2 accessibility bead proves, it does not add): `ExportAccessibilityTests`: "The tag tree of a day" — `testADaysEntryLinesAreContiguousSoOneListTagCoversAllOfThem`; "One pass per entry" — `testOnePassAccessibilityTextEndsAtWhereWhenContextIsExcluded` (also `ExportDocumentBuilderTests.testOnePassAccessibilityTextForAStarredEntry`); "Greyscale" — `testTheStarredMarkIsAPlainTextCharacter`; "The PDF at the largest text size" — `testThePDFRendererUsesFixedPointSizesNotDynamicType` (a source guard: no `preferredFont` call, the three fixed point sizes present). Device checks (the epic's device-check bead): "Screen reader on the PDF", "The document language after the spike", "Voice Control", "Largest text size".
- [x] 1.13 mm-t42.13 — safeguarding, "the export control on the safeguarding pages and in safe mode". `ExportControlButton.swift` (renamed from the `ExportStubButton` placeholder), wired into `GPSuggestionPageView.swift` and `NotRightNowPageView.swift` (both now take `store: RecordStore`, threaded through their four call sites); `App/Midmorning/AppLock/SafeModeView.swift` gives safe mode its own control. Device checks (the epic's device-check bead): one per place, opening the export screen from the GP suggestion page, the not-right-now page and safe mode.
- [x] 1.14 mm-t42.20 — wiring: the stage state after Delete-all, and safe mode. `App/Midmorning/AppLock/AppLockRootView.swift` (the `safeMode` phase; `RunningRootView`, and so the reminder scheduler, is never built for it). `RecordTests.LaunchSafetyWiringTests`: "Third launch with an uncleared marker" — `testThirdConsecutiveLaunchWithARealUnclearedMarkerFileEntersSafeMode` (a real marker file, not a fixture boolean); "Store fails to open" — `testARealContainerFailureReportsFailedNotWaiting` (a real `RecordStore` failure). `RecordTests.DeleteAllWiringTests`, over `Record.LocalEraser`, the real engine: "Delete-all" from mm-t21.20 (programme) — `testDeleteAllLeavesNoStageOpenedRowAndNoCardAnswer`; "Delete-all" from mm-t23.13 (regular-eating-plan) — `testDeleteAllLeavesNoTemplateNoDayPlanAndDefaultSlotLabels`; "After Delete-all" from mm-t14.1 (onboarding) — `testAfterDeleteAllTheStoreStartsOnboardingAgain`; "Delete everything after an enrolment change" from mm-t15.9 (app-lock) — `testDeleteEverythingAfterAnEnrolmentChangeReallyErasesTheDirectory`, over a real `AppLockController`. "Delete-all" from mm-t22.12 (weigh-in) was already end to end before this change, `RecordTests.WeighInStoreTests.testDeleteAllLeavesNoWeighIn`. "Side files" from mm-t41.7 stays proven at `FileProtectionTests`' pure `protectionClass(for:)` level; the widget-snapshot half is still `deferred: mm-t25.15`. "Pending requests first" from mm-t41.1 and mm-t41.2, and "Diagnostics" from mm-t41.13, are device checks: `UNUserNotificationCenter.current()` was confirmed to crash outside a real app bundle (`bd memories`, `ununnotificationcenter-crashes-under-swift-test`), so the real notification centre and the Diagnostics screen stay device-only.

## 2. Device checks

- [ ] 2.1 lists every device-only check from the tasks above: "Screen reader on the PDF", "The document language after the spike", "Voice Control", "Largest text size" (mm-t42.11); "Opens the export screen" from the GP suggestion page, the not-right-now page and safe mode (mm-t42.13); "Pending requests first" with a real notification centre and six real pending reminders, twice (Delete-all and Delete from this device), and "Diagnostics" (mm-t42.20). See the epic's device-check bead. Ash does the checks; per CLAUDE.md, a device-check bead does not gate the epic's closure.
