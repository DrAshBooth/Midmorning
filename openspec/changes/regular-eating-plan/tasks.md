# Tasks

Every task's verify command is `swift test --package-path Packages`, folded into `./verify` before each child closes. Each task names its bead, its requirement heading and spec file, and maps each scenario the requirement lists to a test here or to a `deferred: <bead id>` pointer.

## 0. Foundations (P0)

- [x] 0.1 mm-t23.2 — regular-eating-plan, "Slots and planned meals". `Packages/Sources/Plan/Slot.swift`, `PlannedMeal.swift`, `PlanOrdering.swift`. Scenarios: "A slot placed twice" — `SlotsAndPlannedMealsTests.testASlotPlacedTwiceKeepsOneRowAtTheNewTime`; "Slots out of their usual order" — `testSlotsOutOfTheirUsualOrderSortByRecordDayPlace`; "A planned meal after midnight" — `testAPlannedMealAfterMidnightStaysInFridaysRecordDay`; "A later day start" — `testALaterDayStartKeepsTheSameRecordDay`; "A renamed slot keeps its kind and index" — `testARenamedSlotKeepsItsKindAndIndex`.
- [x] 0.2 mm-t23.9 — regular-eating-plan, "The window of a planned meal". `Packages/Sources/Plan/PlanWindow.swift`, `PlanMatch.swift`. Scenarios: "An entry inside the window" — `PlanWindowTests.testAnEntryInsideTheWindowMatches`; "Two entries inside the window" — `testTwoEntriesInsideTheWindowTheEarliestMatches`; "Overlapping windows" — `testOverlappingWindows`; "Three planned meals 30 minutes apart" — `testThreePlannedMealsThirtyMinutesApart`; "A window clipped to the record day" — `testAWindowClippedToTheRecordDay`; "A window clipped to a later day start" — `testAWindowClippedToALaterDayStart`; "An entry at the window's end" — `testAnEntryAtTheWindowsEndMatchesNothing`; "A matched entry deleted" — `testAMatchedEntryDeletedLeavesTheMealMissed`; "An entry after 'Skipped'" — `testAnEntryAfterSkippedStillMatches`.
- [x] 0.3 mm-t23.8 — regular-eating-plan, "A planned day". `Packages/Sources/Plan/PlannedDay.swift`; `RecordStore`'s Day/set-event calls. Scenarios: "Save an edit to today's plan" — `PlannedDayTests.testSaveAnEditToTodaysPlanIsASetDayAndAPlannedDay`; "An entry on a template day" — `testAnEntryOnATemplateDayIsAPlannedDay`; "A set day without an entry" — `testASetDayWithoutAnEntryIsStillAPlannedDay`; "A day the person did not touch" — `testADayThePersonDidNotTouchIsNotAPlannedDay`; "A 'Skipped' answer only" — `testASkippedAnswerOnlyIsNotAPlannedDay`; "A snooze only" — `testASnoozeOnlyIsNotAPlannedDay`; "A paused day in a run" — `testAPausedDayInARunIsNotPlannedButDoesNotBreakTheRun`; "A fasting day on the plan" — `testAFastingDayOnThePlanIsAPlannedDay`; "A day start change keeps the key" — `RecordTests.PlanStoreTests.testADayStartChangeKeepsTheKey`; "The set event row from another device" — `ConflictRulesTests.testSetEventIsStickyAcrossALaterPlanEdit` and `PlanStoreTests.testTheSetEventRowFromAnotherDeviceThroughTheStore`; "Seven planned days over ten record days" — `PlannedDayTests.testSevenPlannedDaysOverTenRecordDays`.

## 1. The plan builder

- [x] 1.1 mm-t23.1 — regular-eating-plan, "The plan builder opens at stage 2" (P1). `Packages/Sources/Plan/PlanBuilderAccess.swift`; `TodayView`'s day-heading menu, gated the same fixture way `GapBand`'s `stage2Open` is. Built here over fixture facts, with no live dependency: "Four recorded days" — `PlanBuilderAccessTests.testFourRecordedDaysShowsNoPlanBuilderControl`; "Five recorded days with gaps" — `testFiveRecordedDaysWithGapsOpensThePlanBuilder`; "A day without entries" — `testADayWithoutEntriesDoesNotCountTowardStage2`. `deferred: mm-t21.23` runs every one of these end to end once `programme-engine` supplies the live stage.
- [x] 1.2 mm-t23.3 — regular-eating-plan, "Place slots in the plan builder" (P1). `Packages/Sources/Plan/PlanBuilderLayout.swift`, `QuietHours.swift`; `App/Midmorning/PlanBuilderView.swift`. Scenarios: "Place a slot" — `PlanBuilderPlacementTests.testPlaceASlotUsesItsDefaultTime`; "Place a renamed slot" — `testPlaceARenamedSlotStillUsesTheSlotsDefaultTime`; "Place the evening snack" — `testPlaceTheEveningSnack`; "Change a time" — `testChangeATime`; "Remove a planned meal" — `testRemoveAPlannedMeal`; "Gap shown between planned meals" — `testGapShownBetweenPlannedMeals`; "No gap on a fasting day" — `testNoGapOnAFastingDay`; "A planned meal inside quiet hours" — `testAPlannedMealInsideQuietHoursShowsAMessageAndKeepsIt`; "Save below the planned meals" — `testSaveBelowThePlannedMealsLayout` (decision 94).
- [x] 1.3 mm-t23.4 — regular-eating-plan, "Rename a slot in the plan builder" (P1). `Packages/Sources/Plan/SlotLabel.swift`; `RecordStore.slotLabel`/`setSlotLabel`; the builder's rename sheet. Scenarios: "Rename a slot" — `RenameSlotTests.testRenameASlot`; "Twenty-one characters" — `testTwentyOneCharactersTruncatesToTwenty`; "The product name" — `testTheProductNameIsRejectedInAnyLetterCase`; "An empty label" — `testAnEmptyLabelRevertsToTheDefault` and `PlanStoreTests.testAnEmptyLabelRevertsToTheDefaultThroughTheStore`; "A rename is not a plan edit" — `testARenameIsNotAPlanEditByConstruction`. "A saved label applies at once" (requirement text, not its own scenario) — `PlanStoreTests.testASavedLabelAppliesAtOnce`.
- [x] 1.4 mm-t23.5 — regular-eating-plan, "The soft rules show and ask" (P1). `Packages/Sources/Plan/SoftRules.swift`; the builder's confirmation dialog. Scenarios: "Too few meals and snacks" — `SoftRulesTests.testTooFewMealsAndSnacks`; "Save anyway" — `testSaveAnywayIsAdvisoryOnly`; "No meals at all" — `testNoMealsAtAll`; "Go back" — `testGoBackChangesNothing`; "A gap over four hours" — `testAGapOverFourHours`; "Two broken rules" — `testTwoBrokenRulesShowBothLines`; "A gap of exactly four hours" — `testAGapOfExactlyFourHoursShowsNoLine`; "A renamed slot counts by its kind" — `testARenamedSlotCountsByItsKind`; "A long gap on a fasting day" — `testALongGapOnAFastingDayShowsNoGapLineButStillTheMealLine`; "A day that meets every rule" — `testADayThatMeetsEveryRuleShowsNoLine`.
- [x] 1.5 mm-t23.6 — regular-eating-plan, "Weekday and weekend templates" (P1). `Packages/Sources/Plan/Materialisation.swift`; `RecordStore.templateSlotsJSON`/`setTemplateSlotsJSON`/`materialiseDayFromTemplate`. Built here: "A weekend record day" — `TemplatesTests.testAWeekendRecordDayUsesTheWeekendTemplate`; "A template change during the day" — `PlanStoreTests.testATemplateChangeDuringTheDayDoesNotChangeTodaysPlan`; "Copy to the weekend" — `TemplatesTests.testCopyToTheWeekendCopiesTheSameSixPlannedMeals` and the builder's "Copy to weekend plan" button; "Three days without opening the app" — `PlanStoreTests.testThreeDaysWithoutOpeningTheAppMaterialisesEachElapsedDay`. `deferred: mm-t41b.11` — "A Day row arrives by import" (data-and-privacy's own conflict rule already covers the store side; mm-t41b.11 runs it end to end with real import).
- [x] 1.6 mm-t23.7 — regular-eating-plan, "Edit tonight for tomorrow, or this morning for today" (P1). `Packages/Sources/Plan/PlanEditing.swift`; the builder's "Today's plan"/"Tomorrow's plan" modes and locked rows. Scenarios: "Tonight for tomorrow" — `PlanStoreTests.testTonightForTomorrow`; "This morning for today" — `testThisMorningForToday`; "A planned meal with an entry" — `EditTodayTomorrowTests.testAPlannedMealWithAnEntryStaysAsItIs`; "Tomorrow after midnight" — `PlanStoreTests.testTomorrowAfterMidnight`.

## 2. Today

- [x] 2.1 mm-t23.10 — regular-eating-plan, "Today shows the plan beside the record" (P1). `Packages/Sources/Plan/PlannedMealRow.swift`; `App/Midmorning/PlanTodayModel.swift`, `PlannedMealRowView.swift`, `DaySection.displayItems`. Scenarios: "A matched planned meal" — `TodayRowsTests.testAMatchedPlannedMealShowsTheEntryBesideTheSlot`; "A planned meal still to come" — `testAPlannedMealStillToComeShowsTheSlotAndTimeOnly`; "A skipped planned meal" — `testASkippedPlannedMealShowsSkipped`; "Skipped, then an entry matches" — `testSkippedThenAnEntryMatchesShowsTheEntryNotSkipped`; "An entry outside every window" — `testAnEntryOutsideEveryWindowMatchesNoSlot`.
- [x] 2.2 mm-t23.11 — regular-eating-plan, "A missed planned meal gets one prompt" (P1). First cut: "Skipped" and "Add it" only. `Packages/Sources/Plan/MissedMealPrompt.swift`; `PlannedMealRowView`'s prompt buttons. Built here: "The window ends with no entry" — `MissedMealPromptTests.testTheWindowEndsWithNoEntry`; "Add it" — `testAddItUsesThePlannedMealsOwnTime` (the screen wiring is `PlannedMealRowView`'s `onAddIt` passing `PlanRowModel.sortTime` to `NewEntryView.initialTime`); "An unmatched entry after the window" — `testAnUnmatchedEntryAfterTheWindowShowsTheWasThatForm` (the form and its candidate time show; "That was it" is a visible, unwired placeholder, the record-full "Get support" pattern); "Two missed planned meals" — `testTwoMissedPlannedMealsShowThePromptOnTheLatestOnly`; "The prompt moves to a later missed planned meal" — `testThePromptMovesToALaterMissedPlannedMeal`; "An answered prompt, then an earlier missed planned meal" — `testAnAnsweredPromptThenAnEarlierMissedPlannedMealShowsNoPrompt`; "A later entry" — `testALaterEntrySuppressesTheEarlierPrompt`; "The window ends inside quiet hours" — `testTheWindowEndsInsideQuietHoursShowsNoPrompt`; "Skipped from the reminder" — `testSkippedFromTheReminderShowsNoPrompt`; "Unanswered at the end of the day" — `testUnansweredAtTheEndOfTheDayHidesThePrompt`. `deferred: mm-t33.14` — "That was it" and "That was it, then a later window moves over the entry" (3.3 problem-solving wires the match action).
- [x] 2.3 mm-t23.12 — regular-eating-plan, "The next-planned-meal line" (P1). `Packages/Sources/Plan/NextPlannedMeal.swift`; `PlanTodayModel.swift`'s trailing-line case. Scenarios: "Skip lunch" — `NextPlannedMealTests.testSkipLunch`; "Skip lunch from the reminder" — `testSkipLunchFromTheReminder`; "Skip the last planned meal of the day" — `testSkipTheLastPlannedMealOfTheDay`; "A starred entry between planned meals" — `testAStarredEntryBetweenPlannedMeals`; "A starred entry that matches a planned meal" — `testAStarredEntryThatMatchesAPlannedMeal`; "The next planned meal arrives" — `testTheNextPlannedMealArrivesHidesTheLine`; "The window ends" — `testTheWindowEndsHandsOffToTheMissedPrompt`.

## 3. Data and accessibility

- [x] 3.1 mm-t23.13 — regular-eating-plan, "The plan's data stays on the device" (P2). Built here: "Close and open the app" — `PlanStoreTests.testCloseAndOpenTheAppKeepsTheWeekdayTemplate`; "No network" — `testNoNetworkEditingWorksOffline`; "A rename on two devices" — `testARenameOnTwoDevicesTheLaterOneWins`. Built over fixture facts: "Delete-all" — `testDeleteAllLeavesNoTemplateNoDayAndDefaultLabels` (plan rows live in `Record.store`, the directory `DeleteAllSeam` deletes whole); `deferred: mm-t42.20` runs the real erasure end to end.
- [x] 3.2 mm-t23.14 — regular-eating-plan, "Accessibility of the plan" (P2). Read before 2.1/2.2/2.3 above, per CLAUDE.md. `Packages/Sources/Plan/PlannedMealRow.swift` (`PlannedMealAccessibility`), `PlanBuilderAccessibility.swift`. Scenarios: "Label of a matched planned meal" — `AccessibilityOfThePlanTests.testLabelOfAMatchedPlannedMeal`; "Label of a skipped planned meal" — `testLabelOfASkippedPlannedMeal`; "Label of a planned meal without an entry" — `testLabelOfAPlannedMealWithoutAnEntry`; "Label of a planned meal with the prompt" — `testLabelOfAPlannedMealWithThePrompt`; "Labels in the builder" — `testLabelsInTheBuilder`; "Label of a renamed planned meal" — `testLabelOfARenamedPlannedMeal`; "Largest text size" — device check, listed on mm-t23.15.

## 4. Device checks

- [ ] 4.1 mm-t23.15 lists every device-only check from the tasks above (VoiceOver reading order and custom actions on the plan rows and the builder, the largest-text-size check, and the shame walk, which also covers the missed planned meal prompt and the next-planned-meal line). See the epic's device-check bead. Ash closes it after the checks; it does not gate the epic (CLAUDE.md, "Worktrees and beads").
