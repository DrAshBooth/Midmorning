# Tasks

## 1. Package

- [x] 1.1 Create `Packages/Record` with product `RecordCore`, tools version 6.0, platforms macOS 14 and iOS 17. Verify: `swift build --package-path Packages/Record` succeeds.
- [x] 1.2 Add the `Entry` model with the six stored values, inline defaults, no unique attribute, and `.allowsCloudEncryption` on every attribute except `id`. Verify: the build succeeds.
- [x] 1.3 Add `RecordDay.interval(containing:calendar:)` with the 04:00 rule from the design. Verify: the build succeeds.
- [x] 1.4 Add `RecordStore` with `add(time:what:feltLikeABinge:createdAt:)` and `entries(recordDayContaining:calendar:)`. `add` trims What, truncates time to the minute, saves and throws with no entry data. Verify: the build succeeds.
- [x] 1.5 Add `Entry.clockTime` and `Entry.accessibilityLabel`. Verify: the build succeeds.
- [x] 1.6 Write one `@MainActor` test. It creates a temporary directory and opens a container with `cloudKitDatabase: .none`. It uses a Gregorian calendar with time zone Europe/London. Verify: the test file compiles.
- [x] 1.7 In the test, add these entries on Thursday 24 September 2026 with fixed `createdAt` values: 13:05 "Toast and tea" starred, created 13:08; 08:10 "Porridge" created 09:00; 00:30 on 25 September "Crisps" created 00:31; 03:59 on 24 September "Soup" created 04:00; 04:00 on 24 September "Tea" created 04:01; 13:05 "" created 13:07. Verify: the test compiles.
- [x] 1.8 In the test, release the container and open a second one on the same file. Verify: the second container opens.
- [x] 1.9 In the test, query the record day at 13:30 on 24 September and again at 01:00 on 25 September. Assert both return, in order: 04:00 "Tea", 08:10 "Porridge", 13:05 "" (created 13:07), 13:05 "Toast and tea" starred, 00:30 "Crisps". Assert "Soup" is absent. Verify: `swift test --package-path Packages/Record` passes.
- [x] 1.10 In the test, assert each returned entry's `what`, `feltLikeABinge` and `createdAt` match the values the test added, and that `add` with "  Toast and tea " stores "Toast and tea". Verify: `swift test` passes.
- [x] 1.11 In the test, assert `clockTime` of an entry saved at 20:00 with the London offset reads "20:00" when the calendar's zone is America/New_York. Assert the label of the starred entry is "13:05, Toast and tea, felt like a binge" and the label of the empty entry is "13:05". Verify: `swift test` passes.
- [x] 1.12 In the test, assert the interval containing 01:00 on Sunday 25 October 2026 in London starts at 04:00 on Saturday 24 October and is 25 hours long. Verify: `swift test` passes.

## 2. App

- [x] 2.1 Create `App/Midmorning.xcodeproj` with synchronized folders, iOS 17 target, bundle identifier `uk.midmorning.app`, a link to `RecordCore`, `Midmorning.entitlements` with `group.uk.midmorning`, `PrivacyInfo.xcprivacy`, and `ITSAppUsesNonExemptEncryption` set to `NO`. Verify: `xcodebuild -project App/Midmorning.xcodeproj -scheme Midmorning -destination 'generic/platform=iOS Simulator' build` succeeds.
- [x] 2.2 Check the built app's entitlements. Verify: the built binary's `__TEXT,__entitlements` section (`otool -s __TEXT __entitlements`) holds `com.apple.security.application-groups` with `group.uk.midmorning`. A simulator build carries entitlements there, not in the code signature.
- [x] 2.3 At launch, create `Library/Application Support` in the group container with `NSFileProtectionComplete`, set `isExcludedFromBackup` on it, and open the container on `Record.store` there with `cloudKitDatabase: .none`. Verify: on the simulator, the file exists under a path that contains `/Containers/Shared/AppGroup/`, and the app prints no entry data.
- [x] 2.4 Add the Today screen with the heading rule, the two-day query, rows that show `clockTime`, What when not empty, and an asterisk on starred rows in the time's colour and weight. Verify: the build succeeds; on the simulator, entries from a seeded store appear in the specified order; screenshot `evidence/2.4-today.png`.
- [ ] 2.5 Make each row one accessibility element with `Entry.accessibilityLabel`, use system text styles, and set no line limit on What. Verify: Accessibility Inspector on the simulator shows the label "13:05, Toast and tea, felt like a binge" on the starred row; screenshot `evidence/2.5-inspector.png`. Code written and the label is asserted in the package test; the Inspector check is not yet performed.
- [ ] 2.6 Add the control that opens the new-entry screen in one tap and recompute the record day on activation and at 04:00. Verify: on the simulator, one tap opens the sheet; changing the simulator clock past 04:00 and reactivating the app changes the heading. Not yet performed: needs a hand on the simulator.
- [ ] 2.7 Add the new-entry screen: a date-and-time control with range from the start of the previous record day to now, defaulting to the open moment; What with no placeholder and focus on open; the star control labelled "felt like a binge"; "Save" and "Cancel"; no title. Verify: on the simulator with the hardware keyboard disconnected, the keyboard is up on open; the control refuses a time before the previous record day; screenshot `evidence/2.7-new-entry.png`. Code written; not yet performed: needs a hand on the simulator.
- [ ] 2.8 On save, close the sheet with the standard dismissal, show Today and scroll to the saved entry. Add no message, colour change, icon, sound, haptic or animation. Verify: on the simulator, save "Toast and tea" and see it on Today with nothing else shown; save with an empty What and see the time alone. Code written; not yet performed: needs a hand on the simulator.
- [ ] 2.9 Add `.privacySensitive()` to Today and the new-entry screen and redact when the scene is not active. Block third-party keyboards on the new-entry screen. Verify: on the simulator, the app switcher shows no entry text; screenshot `evidence/2.9-switcher.png`. Code written; not yet performed: needs a hand on the simulator.
- [x] 2.10 Save an entry on the simulator, quit the app, open it again. Verify: the entry is still on Today.
- [x] 2.11 Set the simulator to the largest accessibility text size. Verify: Today and the new-entry screen show all text without truncation; screenshot `evidence/2.11-large-text.png`.
- [ ] 2.12 Walk the starred-entry path on the built app and answer the shame question for each screen. Verify: the change's README records who walked it, the date and each answer.
