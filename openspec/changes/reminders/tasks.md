# Tasks

Every task's verify command is `swift test --package-path Packages`, folded into `./verify` before each child closes. Each task names its bead, its requirement heading and spec file, and maps each scenario the requirement lists to a test here or to a `deferred: <bead id>` pointer.

## 0. Foundations (P0)

- [x] 0.1 mm-t24.17 — reminders, "The scheduler pipeline". `Packages/Programme/Reminders/Scheduler.swift` (`Scheduler.pipeline`), `ReminderCap.swift`, `SameMinuteShift.swift`. Built here: "A switch outranks the end of a pause" — `SchedulerPipelineTests.testASwitchOutranksTheEndOfAPause`; "A paused day outlasts the end of a pause" — `testAPausedDayOutlastsTheEndOfAPause`; "A dropped reminder takes no part in the shift" — `testADroppedReminderTakesNoPartInTheShift`; "A shifted reminder lands in quiet hours" — `testAShiftedReminderLandsInQuietHours`; "A quiet-hours drop still counts toward the cap" — `testAQuietHoursDropStillCountsTowardTheCap`. `deferred: mm-t36.21` — "A paused day after the finish", "The reduced cadence runs before the cap".
- [x] 0.2 mm-t24.18 — reminders, "Scheduling is local, lazy and bounded". `Scheduler.swift` (`Scheduler.requests`), `ReminderPrimitives.swift`. Built here: "No network"/"The app is not running"/"Device restart" — `SchedulingLocalLazyBoundedTests.testTheSchedulerHasNoNetworkOrProcessDependency`; "Six days ahead" — `testSixDaysAheadAndTheFarReminder`; "The far reminder" — `testSixDaysAheadAndTheFarReminder`; "Three days without opening the app" — `testThreeDaysWithoutOpeningTheApp`; "A later day start" — `testALaterDayStartPlacesTheTimeOnTheSuppliedDayStart`; "A time-zone change" — `testATimeZoneChange`. Built here over fixture facts: "A synced setting arrives by push" — a changed-settings recompute is simply another `Scheduler.requests` call with new `SchedulerSettings`, proved by `testTheSchedulerHasNoNetworkOrProcessDependency`'s own determinism; `mm-t41b.13` runs it end to end with a real CloudKit push. `deferred: mm-t36.17` — "The background refresh task runs", "The background refresh task never runs" (the task itself is not registered in the first cut, per `deferred.md`).
- [x] 0.3 mm-t25.9 — widgets-and-intents, "The action queue". `Packages/Sources/Record/ActionQueue.swift` (`ActionQueueCodec`), `RecordStore.applyQueuedActions`. Built here: "Skipped while the app is closed" — `ActionQueueStoreTests.testSkippedWhileTheAppIsClosed`; "Queue content" — `testQueueContent`; "Action from an earlier day" — `testActionFromAnEarlierDay`; "Two actions in order" — `testTwoActionsInOrder`; "Snooze count applied" — `testSnoozeCountApplied`. `deferred: mm-t25.15` — "Entry from a widget".

## 1. Reminder content and types

- [x] 1.1 mm-t24.1 — reminders, "Reminder types and their switches". `ReminderSettingsSupport.swift` (`ReminderPermissionText`), `RemindersSettingsView.swift`, `TodayView.swift`. Built here: "Turn off planned meal reminders" — `ReminderTypesAndSwitchesTests.testTurnOffPlannedMealReminders`; "Permission not determined" — `testPermissionNotDetermined`; "The line before permission" — `testTheLineBeforePermission`; "Permission granted from the line" — `testPermissionGrantedFromTheLine`; "Permission denied" (except "Show me the widget", `deferred: mm-t25.15`) — `testPermissionDenied`; "The line on Today" — `testTheLineOnToday`; "The line after one tap" — `testTheLineAfterOneTap`; "Turn a type back on" — `testTurnATypeBackOn`. Built here over fixture facts: "Two devices" — `testTwoDevices` (the switch is already a `Local.store` device row; `RecordStore.ReminderSwitch`, mm-t13). `deferred: mm-t33.15` — "Worksheet review off".
- [x] 1.2 mm-t24.2 — reminders, "A reminder for every planned meal". `Scheduler.candidates(for:settings:)`. Scenarios: "A planned meal at its time" — `ReminderForEveryPlannedMealTests.testAPlannedMealAtItsTime`; "The plan changes" — `testThePlanChanges`; "An entry before the time" — `testAnEntryBeforeTheTime`; "An entry after the reminder" — `testAnEntryAfterTheReminder`; "A day beyond tomorrow" — `testADayBeyondTomorrow`.
- [x] 1.3 mm-t24.3 — reminders, "Actions on a planned meal reminder". `ReminderPrimitives.swift` (`PlannedMealReminderAction`), `SnoozeDecision.swift` (`ReminderUserInfo`), `App/Midmorning/Reminders/NotificationCategories.swift`, `NotificationActionHandling.swift`. Scenarios: "Add" — `ActionsOnAPlannedMealReminderTests.testAdd`; "The three actions" — `testTheThreeActions`; "The three actions with explicit wording on" — `testTheThreeActionsWithExplicitWordingOn`; "Skipped"/"Skipped from the Lock Screen"/"A snooze from the Lock Screen"/"Add from the Lock Screen" — `testTheDeclarativeOptionsPerAction` (the declarative options `NotificationCategories.registerAll` sets); "The slot index in the userInfo" — `testTheSlotIndexInTheUserInfo`; "A tap on the reminder" — `testATapOnTheReminder` (App-target routing: a plain tap's default identifier opens Today, `NotificationActionHandling`'s own default case).
- [x] 1.4 mm-t24.4 — reminders, "Snooze a reminder". `SnoozeDecision.swift`, `RecordStore.snoozeCount`/`setSnoozeCount`. Scenarios: "Remind me in 15 minutes" — `SnoozeAReminderTests.testRemindMeIn15Minutes`; "The thirty-minute title" — `testTheThirtyMinuteLength`; "A third snooze" — `testAThirdSnooze`; "A snooze past the next planned meal" — `testASnoozePastTheNextPlannedMeal`; "A snooze into quiet hours" — `testASnoozeIntoQuietHours`; "The pure function" — `testThePureFunction`; "A snooze survives a restart" — `ActionQueueStoreTests.testASnoozeSurvivesARestart` and `testSnoozeCountApplied`.
- [x] 1.5 mm-t24.5 — reminders, "Discreet text by default". `ReminderPrimitives.swift` (`DiscreetText`). Built here: "A discreet planned meal reminder" — `DiscreetTextByDefaultTests.testADiscreetPlannedMealReminder`; "An explicit planned meal reminder" — `testAnExplicitPlannedMealReminder`; "An explicit reminder for a renamed slot" — `testAnExplicitReminderForARenamedSlot`; "A renamed slot with explicit wording off" — `testARenamedSlotWithExplicitWordingOff`; "An explicit close-the-day reminder" — `testAnExplicitCloseTheDayReminder`; "The reminder sound" — `testTheReminderSoundIsAlwaysTheSystemDefault`; "The app icon" — `testTheAppIconNeverCarriesABadge`. `deferred: mm-t36.18` — "An explicit check-in reminder" (the title text itself is proved by `testAnExplicitCheckInReminderTitle`; scheduling the check-in reminder is `staying-on-track`'s own work).
- [x] 1.6 mm-t24.6 — reminders, "Delivered reminders are grouped and removed". `ReminderSettingsSupport.swift` (`DeliveredReminderRemoval`), `App/Midmorning/Reminders/DeliveredReminderCleanup.swift`. Scenarios: "One thread" — `DeliveredRemindersGroupedAndRemovedTests.testOneThread`; "The window ends" — `testTheWindowEnds`; "The end of the record day" — `testTheEndOfTheRecordDay`; "A later day start" — `testALaterDayStart`; "Launch" — `testLaunchRemovesEveryDeliveredReminder` and `DeliveredReminderCleanup.removeAllAtLaunch` (App target).

## 2. Day rules

- [x] 2.1 mm-t24.7 — reminders, "The morning plan reminder while the plan needs setting". `DayRules.swift` (`MorningPlanFacts`/`Rule`/`UnansweredTracker`). Built here: "A template exists" — `MorningPlanReminderTests.testATemplateExists`; "An edit the day before" — `testAnEditTheDayBefore`; "A template change" — `testATemplateChange`; "Tomorrow set tonight" — `testTomorrowSetTonight`; "Three unanswered days" — `testThreeUnansweredDays`; "Days with no delivered reminder" — `testDaysWithNoDeliveredReminder`. Built here over fixture facts: "No template yet" — `testNoTemplateYet`; "Stage 1" — `testStage1` (`mm-t32.16` runs both end to end).
- [x] 2.2 mm-t24.8 — reminders, "Close the day". `DayRules.swift` (`CloseTheDayFacts`/`Rule`), `RecordStore.feelingWord`/`setFeelingWord`, `App/Midmorning/CloseTheDayView.swift`. Built here: "The reminder" — `CloseTheDayTests.testTheReminder`; "The last planned meal has an entry" — `testTheLastPlannedMealHasAnEntry`; "The last planned meal is missed" — `testTheLastPlannedMealIsMissed`; "An entry after the last planned meal's time" — `testAnEntryAfterTheLastPlannedMealsTime`; "Add an entry" — `CloseTheDayView`'s "Add an entry" button opens `NewEntryView`; "One word" — `ActionQueueStoreTests.testOneWord`; "A day with no entries" — `ActionQueueStoreTests.testNoFeelingWordYet` and the screen's fixed field set. Built here over fixture facts: "An evening entry in stage 1" — `testAnEveningEntryInStage1` (`mm-t32.16` runs it end to end).
- [x] 2.3 mm-t24.9 — reminders, "The midday reminder". `DayRules.swift` (`MiddayFacts`/`Rule`). Built here: "The explicit midday reminder" — `MiddayReminderTests.testTheExplicitMiddayReminder`; "The midday time on the settings screen" — `testTheMiddayTimeOnTheSettingsScreen` (`RemindersSettingsView`'s existing `settings.reminders.middayCaption`, mm-t13); "An entry in the morning" — `testAnEntryInTheMorning`; "A fasting day" — `testAFastingDay`; "A plan with a morning planned meal" — `testAPlanWithAMorningPlannedMeal`; "A day with no entries at all" — `testADayWithNoEntriesAtAll`. Built here over fixture facts: "No entry by midday in stage 1" — `testNoEntryByMiddayInStage1` (`mm-t32.16` runs it end to end).
- [x] 2.4 mm-t24.10 — reminders, "The midday and close-the-day reminders stop after seven silent days". `DayRules.swift` (`SilentDayOutcome`/`Tracker`), `RecordStore.silentDayStreak`/`setSilentDayStreak`. Scenarios: "Seven silent days" — `SilentDaysTests.testSevenSilentDays`; "An entry ends the run" — `testAnEntryEndsTheRun`; "An entry after the stop" — `testAnEntryAfterTheStop`; "A paused day in the run" — `testAPausedDayInTheRun`; "The switches stay as they were" — `testTheStopChangesNoSwitch`.
- [x] 2.5 mm-t24.13 — reminders, "The cap of two other reminders a day". `ReminderCap.swift`. Built here: "A weekday with three other reminders" — `CapOfTwoOtherRemindersTests.testAWeekdayWithThreeOtherReminders`; "The weekly review on the weigh-in day" — `testTheWeeklyReviewOnTheWeighInDay`; "Six planned meals and two others" — `testSixPlannedMealsAndTwoOthers`; "Snoozes do not count" — `testSnoozesDoNotCount`. `deferred: mm-t33.15` — "A worksheet review moves a day".
- [x] 2.6 mm-t24.14 — reminders, "Two reminders never share a minute". `SameMinuteShift.swift`. Scenarios: "The weigh-in day at the morning plan time" — `TwoRemindersNeverShareAMinuteTests.testTheWeighInDayAtTheMorningPlanTime`; "Three at one minute" — `testThreeAtOneMinute`.
- [x] 2.7 mm-t24.15 — reminders, "Quiet hours". `ReminderPrimitives.swift` (`ReminderQuietHours`). Scenarios: "A planned meal inside quiet hours" — `QuietHoursTests.testAPlannedMealInsideQuietHours`; "The edges of the range" — `testTheEdgesOfTheRange`; "A reminder outside quiet hours" — `testAReminderOutsideQuietHours`; "A close-the-day time inside quiet hours" — `testACloseTheDayTimeInsideQuietHours`; "Quiet hours off" — `testQuietHoursOff`.
- [x] 2.8 mm-t24.16 — reminders, "A paused day and a reminder pause silence everything". `Scheduler.swift` (`candidates(for:settings:)`, `pipeline`). Scenarios: "Pause at 14:00" — `PausedDaySilenceTests.testPauseAt1400`; "The day after a pause" — `testTheDayAfterAPause`; "A reminder pause" — `testAReminderPause`; "Resume" — `testResume`; "The self-harm reason" — `testTheSelfHarmReason`.

## 3. Cross-capability (app-lock, widgets-and-intents)

- [x] 3.1 mm-t24.19 — app-lock, "A new entry before authentication" (the notification-action scenarios). `App/Midmorning/Reminders/NotificationActionHandling.swift`, `NotificationCategories.swift`, `App/Midmorning/AppLock/AppLockRootView.swift` (the `.reminderAddActionTapped` route). Scenarios: "Notification action while locked" — `AppLockTests.AppLifecycleTests.testAPendingRouteShowsWithNoCoverEvenWhileLocked` (already built by `mm-t15.6`); "Actions on a planned meal reminder" — `AppLockNotificationActionsWhileLockedTests.testActionsOnAPlannedMealReminder`; "Skipped while the app is locked"/"Skipped on the locked device" — `testSkippedNeverOpensTheAppAndAlwaysNeedsTheDeviceUnlocked`; "Snooze while the device is locked" — `testSnoozeNeedsNoUnlockAndNeverOpensTheApp`. `deferred: mm-t25.15` — "Widget tap while locked", "Save while locked", "Cancel the request at Save", "Cancel on the screen".
- [x] 3.2 mm-t25.8 — widgets-and-intents, "Notification actions are entry points". Same App-target files as 3.1. Scenarios: "Add" — `NotificationActionsAreEntryPointsTests.testAdd`; "Actions in order" — `testActionsInOrder`; "Skipped" — `testSkipped`; "Skipped on the locked device" — `testSkippedOnTheLockedDevice`; "Snooze" — `testSnooze`; "userInfo content" — `testUserInfoContent`.

## 4. Settings and wiring

- [x] 4.1 mm-t24.20 — settings, "The About group" (the two Diagnostics counts only; no scenario claimed — `mm-t41`'s settings child wires the real page). `ReminderSettingsSupport.swift` (`ReminderDiagnostics`). `DiagnosticsCountsTests.testPendingReminderCount`, `testQueueLength`.
- [x] 4.2 mm-t24.21 — wiring: the real scheduler under settings, onboarding and safeguarding. `App/Midmorning/Reminders/ReminderCoordinator.swift` gathers real `RecordStore`/`Plan` facts into `SchedulerDay`/`SchedulerSettings` and calls `Scheduler.requests`, then replaces every pending local notification request; called from `RemindersSettingsView` (every switch, time and quiet-hours change, and "Turn reminders on") and from `AppLockRootView`'s activation. `TodayView`'s lock control now dispatches `AppLifecycleEvent.lockControlTapped` on the real, shared `AppLockController` instead of a no-op placeholder. Scenarios: "Reminders back on" and "Nothing closes" (safeguarding) — the same `remindersPausedAt: nil` recompute path `PausedDaySilenceTests.testResume` already proves; neither safeguarding page needed a change. "Notifications denied"/"Notifications not asked" (onboarding) — `WiringSchedulerUnderSettingsOnboardingSafeguardingTests.testNotificationsDeniedGivesNoPendingRequest`/`testNotificationsNotAskedGivesNoPendingRequest`, over the new `SchedulerSettings.notificationPermissionGranted` gate. "Reminders paused by the not-right-now page" (settings) — already correct: `RemindersSettingsView.load()` already reads the real `remindersPausedAt` state (mm-t13). "Turn reminders on" (settings) — `testTurnRemindersOnIsTheSameResumePathAlreadyProved`; the real `ReminderCoordinator.recomputeAndApply` call is App-target code with no `swift test` target, so the epic's device-check bead lists the on-device proof. "Lock at once"/"Lock control with the app lock off" (app-lock) — `AppLockTests.AppLifecycleTests.testAPendingRouteShowsWithNoCoverEvenWhileLocked`-adjacent reducer tests already prove the pure rule; the lock button itself is now wired for real. "Face ID only" (settings) — already proved by `mm-t15.13`'s own `AuthenticationPolicyTests.testFaceOrTouchOnlySelectsTheBiometricsOnlyPolicy` (a spec-hygiene nit put this scenario under the wrong heading; see the `settings-spec-face-id-only-scenario-misplaced` memory). Every scenario that needs a device (the real permission dialog's effect on the schedule, the lock button's real cover, a real recompute after "Turn reminders on") is listed on the epic's device-check bead.

## 5. Device checks

- [ ] 5.1 mm-t24.22 lists every device-only check from the tasks above (the real permission dialog, "Skipped"/"Add" while locked, a live snooze, Notification Centre grouping and removal timing, the largest-text-size check, VoiceOver's reading order and custom actions, and the shame walk, which also covers a reminder on the Lock Screen at work). See the epic's device-check bead. Ash closes it after the checks; it does not gate the epic (CLAUDE.md, "Worktrees and beads").
