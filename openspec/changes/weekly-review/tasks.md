# Tasks

Every task's verify command is `swift test --package-path Packages` (and, where noted, `xcodebuild ... build`), folded into `./verify` before each child closes. Each task names its bead, its requirement heading and spec file, and maps each scenario the requirement lists to a test here or to a `deferred: <bead id>` pointer.

## 1. The pure rules and the store (P1)

- [x] 1.1 mm-t32.3 — weekly-review, "When a weekly review is due". `Packages/Programme/WeeklyReview/ReviewDue.swift`. Built here, `ReviewDueTests`: "The first review" — `testTheFirstReview`; "Today while a review is due" — `testTodayWhileAReviewIsDue`; "A review left unfinished" — `testAReviewLeftUnfinished`; "No network" — `RecordTests.ReviewStoreTests.testNoNetworkReviewSavesLocally` (the save path calls no network API). "After the finish" — `deferred: mm-t36.15`.
- [x] 1.2 mm-t32.6 — weekly-review, "The week's counts are frozen in the Review row". `Packages/Programme/WeeklyReview/ReviewFreeze.swift`, `ReviewFacts.swift`; `RecordStore.review`/`.reviewRowWinners`/`.upsertReview` (`RecordStore.swift`). Built here: "An entry deleted after the review is built" — `ReviewFreezeTests.testAnEntryDeletedAfterTheReviewIsBuilt`; "A review the person never opens" — `testAReviewThePersonNeverOpens`; "The device clock moves back" — `testTheDeviceClockMovesBack`; "A row with a due moment far ahead" — `testARowWithADueMomentFarAhead`; "Keyed by the due day" — `RecordTests.ReviewStoreTests.testKeyedByTheDueDay`. Built here, over fixture facts, no live dependency: "Two devices freeze the same review" — `RecordTests.ReviewStoreTests.testTwoDevicesFreezeTheSameReview`; "Sync behind the due moment" — `ReviewFreezeTests.testReadyToFreezeWaitsForTheDueMoment`/`.testReadyToFreezeWaitsForSyncWhenSyncIsOn` plus `RecordTests.ReviewStoreTests.testSyncBehindTheDueMomentWritesNoFrozenRowYet`; `mm-t41b.13` runs both end to end. Bug fixed while building this: `Record.Reconciler.ReviewReconciler.winners` picked the earliest-frozen row with no secondary tiebreak; two rows sharing one `frozenAt` (an edit that correctly carries the freeze moment forward) resolved arbitrarily. Fixed to break the tie on the latest `changedAt`; `RecordTests.ReviewStoreTests.testAnEditWritesIntoTheFrozenRow` proves it, and the existing `DayStateSessionReviewTests` still pass unchanged.
- [x] 1.3 mm-t32.5 — weekly-review, "The summary built from the record". `Packages/Programme/WeeklyReview/ReviewSummary.swift`. Built here, `ReviewSummaryTests`: "A full week" — `testAFullWeek`; "The first review" — `testTheFirstReview`; "A skipped planned meal" — `testASkippedPlannedMeal`; "Words in the person's order" — `testWordsInThePersonsOrder`; "An entry after midnight" — `testAnEntryAfterMidnight`; "A 'didn't record' day" — `testADidntRecordDay`; "A fasting day" — `testAFastingDay`; "I won't be weighing" (the review half) — `testIWontBeWeighing`; "Zero starred entries" — `testZeroStarredEntries`. Deferred: `mm-t36.21` builds "A check-in", "A check-in with no entries", and the check-in half of "I won't be weighing".
- [x] 1.4 mm-t32.2 — safeguarding, "The deterioration rule". `Packages/Programme/Safeguarding/DeteriorationRule.swift`. All 6 scenarios, `DeteriorationRuleTests`: "Three rising weeks" — `testThreeRisingWeeks`; "Two rising weeks" — `testTwoRisingWeeks`; "Rising from a low count" — `testRisingFromALowCount`; "Rising but not doubled" — `testRisingButNotDoubled`; "I'm getting worse" — the rule reports the same result from the same counts regardless of trigger, `testFewerThanFourReviewsNeverFires`; "Getting worse after the rule fired" — `testGettingWorseAfterTheRuleFiredIsIdempotent`. The page-shown-at-most-once gate is `ReviewScreenView.checkDeteriorationOnce` (App target; see 2.2).
- [x] 1.5 mm-t32.7 — weekly-review, "The deterioration rule at the review". Reads `DeteriorationRule.fires` (1.4); no second rule. All 3 scenarios, `DeteriorationRuleAtTheReviewTests`: "Three rising weeks" — `testThreeRisingWeeks`; "Rising but small" — `testRisingButSmall`; "Rising but not doubled" — `testRisingButNotDoubled`.
- [x] 1.6 mm-t32.8 — weekly-review, "The weekly summary is opt-out". `RecordStore.weeklySummaryOn`/`.setWeeklySummaryOn` (`RecordStore.swift`); `SettingsView.swift` (the "Weekly summary" switch); `WeeklyReviewModel.summary` reads it. All 3 scenarios: "Switch off" — the summary omits every part when the switch is off, `WeeklyReviewModel.summary`'s own guard, proved by `ReviewSummaryTests` (an empty-facts summary shows only the fixed parts) plus `RecordTests.ReviewStoreTests.testWeeklySummaryDefaultsOnAndSyncs`; "Switch off, safeguarding still counts" — `RecordTests.ReviewStoreTests.testSwitchOffSafeguardingStillCounts`; "Switch on again" — the same guard, symmetric.
- [x] 1.7 mm-t32.9 — weekly-review, "Three reflection questions". `Packages/Programme/WeeklyReview/ReviewContent.swift` (`reflectionQuestions`). All 3 scenarios: "The three questions" — the fixed, ordered array itself, `ReviewAccessibilityTests.testNoFixedWidthContent` asserts every question is present and non-empty; "Answer one question" — `RecordTests.ReviewStoreTests.testAnEditWritesIntoTheFrozenRow` (an answer round-trips through `ReviewAnswersPayload`); "Same questions each week" — the array is a `static let`, never built per week.
- [x] 1.8 mm-t32.10 — weekly-review, "The one thing to change and the pinned note". `Packages/Programme/WeeklyReview/PinnedNoteHold.swift`; `RecordStore.upsertReview`'s `pinnedNote` field; `WeeklyReviewModel.currentPinnedNote`/`.pinnedNoteHeld`. Built here: "A pinned note appears" — `RecordTests.ReviewStoreTests.testAPinnedNoteAppears`; "The next review replaces it" — `testTheNextReviewReplacesIt`; "The next review leaves it empty" — `testTheNextReviewLeavesItEmpty`; "Edit from Today" — `testEditFromToday`; "A starred entry holds the pinned note" — `PinnedNoteHoldTests.testAStarredEntryHoldsThePinnedNote`. Built here, over fixture facts, no live dependency: "A binge outcome holds the pinned note" — `PinnedNoteHoldTests.testABingeOutcomeHoldsThePinnedNote`; `mm-t31.19` runs it end to end once `urge-toolkit` lands.
- [x] 1.9 mm-t32.13 — weekly-review, "Week-1 answers". `Packages/Programme/WeeklyReview/ReviewContent.swift` (`weekOneQuestions`); `WeeklyReviewModel.saveDone`'s `week == 1` guard; `ReviewScreenView`'s `week == 1` section. All 3 scenarios: "The review of week 1" — the fixed question order, `ReviewAccessibilityTests.testNoFixedWidthContent`; "The review of week 2" — `WeeklyReviewModel.saveDone` only writes `weekOneAnswers` when `week == 1`, structural; "Week 1 review never finished" — no row, no answers, the store's own append-only "Done" write is the only writer.
- [x] 1.10 mm-t24.12 — reminders, "The weekly review reminder". `Packages/Programme/Reminders/WeeklyReviewReminderRule.swift`, wired live in `App/Midmorning/Reminders/ReminderCoordinator.swift` (`weeklyReviewCandidates`, the `.weeklyReview` switch), `NotificationActionHandling.swift`/`AppLock/AppLockRootView.swift` (the tap route; `RemindersSettingsView.swift`'s time picker already recomputes after this change's own one-line fix). Built here: "The seventh day" — `WeeklyReviewReminderTests.testTheSeventhDay`; "The review done early" — `testTheReviewDoneEarly`; "Mid-week" — `testMidWeek`. Also `testAtMostOnePerReview`. Deferred: `mm-t36.15` builds "After the finish"; this rule already reads a `nil` `dueDayKey` the same way, `testAfterTheFinish`.
- [x] 1.11 mm-t32.1 — safeguarding, "Re-screening at every weekly review and check-in". The two-step outcome itself is `Programme.SelfHarmItem`, already proved by `SelfHarmItemTests` (onboarding-and-safeguarding, ahead of this change); this bead's own scenarios are the review's placement of it. "Thoughts with a method at review" — `SelfHarmItemTests.testThoughtsWithAMethod` plus `RecordTests.WeeklyReviewWiringTests.testRemindersKeptByTheSelfHarmReason`; "Thoughts without a method at review" — `SelfHarmItemTests.testThoughtsWithoutAMethod`; "No at review" — `SelfHarmItemTests.testNo`; "Review left open" — `RecordTests.ReviewStoreTests.testAskedAgainInTheNextReview`; "Done without an answer" — `testDoneWithoutAnAnswer`; "The store after a review" — `testTheStoreAfterAReviewKeepsNoAnswer`. `mm-t36.8` carries the check-in scenarios.
- [x] 1.12 mm-t32.11 — weekly-review, "The self-harm item at the review". Same underlying component as 1.11, this requirement's own scenario names. "Answer No" — `SelfHarmItemTests.testNo`; "I'd rather not say" — `testRatherNotSay`; "Yes, then No" — `testThoughtsWithoutAMethod`; "Yes, then Yes" — `testThoughtsWithAMethod` plus `RecordTests.WeeklyReviewWiringTests.testSelfHarmReasonAtTheReview`; "Done with no answer" — `RecordTests.ReviewStoreTests.testDoneWithoutAnAnswer`; "Asked again" — `testAskedAgainInTheNextReview`.
- [x] 1.13 mm-t32.12 — weekly-review, "\"I'm getting worse\"". `ReviewScreenView.tapGettingWorse` (App target); reads `GPSuggestionPage.line(for: .gettingWorse)` (already built, onboarding-and-safeguarding). All 5 scenarios: "Tap it" — `ReviewScreenView.saveInProgress` then `gpSuggestionReasons = [.gettingWorse]`, structural (no App-target test runner); "Back to the review" — the page's own "Done" clears `gpSuggestionReasons`, structural; "Leave it alone" — no button tap, no page, structural; "Nothing around it" — the button sits in its own `Section` with no other text, structural; "Getting worse after the rule fired" — `DeteriorationRuleTests.testGettingWorseAfterTheRuleFiredIsIdempotent` proves the rule's own result is independent of the button; `ReviewScreenView.checkDeteriorationOnce`'s own per-session flag is separate from the button's own always-show path.

## 2. The screens (P1)

- [x] 2.1 mm-t32.4 — weekly-review, "Finish and reopen a review". `App/Midmorning/WeeklyReview/ReviewScreenView.swift`, `ReviewsListView.swift`, `WeeklyReviewModel.swift`; `TodayView.swift` (`ReviewsListRoute`, `WeeklyReviewRoute`, the live "Reviews" bottom-bar item). Built here (App-target views, no test runner; each maps to the `RecordStore`/`Programme` composition a test proves): "Done with empty answers" — `RecordTests.ReviewStoreTests.testDoneWithoutAnAnswer` (the store write with empty fields); "Done below the content" — `ReviewScreenView`'s `safeAreaInset(edge: .bottom)` "Done" button and `.getSupport()`'s trailing bar item, structural; "Edit before the next review" — `RecordTests.ReviewStoreTests.testAnEditWritesIntoTheFrozenRow`; "The Reviews list" — `WeeklyReviewModel.reviewsListRows`, `ReviewContent.rowText`, structural (the row text function itself has no test file of its own; its two branches are exercised by `RecordTests.ReviewStoreTests.testSwitchOffSafeguardingStillCounts`'s data and by decision 95's own guard below); "The Reviews list with the summary off" — `ReviewContent.rowTextWithoutCount`, same; "No Reviews control before the first review" — `ReviewDueTests.testNoReviewControlBeforeTheFirstReview`; "The Reviews control appears" — `WeeklyReviewModel.Snapshot.reviewsControlShows`, the same `ReviewDue.latestDueWeek` fact; "Store error" — already proved ahead of this change, `RecordTests.NoRecordContentInErrorsTests`, over the store's one `Failure.saveFailed` case every save (including `upsertReview`) throws.
- [x] 2.2 mm-t32.16 — wiring: the weekly review, and the stage engine with the scheduler, end to end. `RecordTests.WeeklyReviewWiringTests`, over the real `RecordStore` and `Programme`: "Reminders kept by the self-harm reason" — `testRemindersKeptByTheSelfHarmReason`; "Self-harm reason" — `testSelfHarmReasonAtTheReview`; "At the review" — `testAtTheReviewGPSuggestionPage`; "Review without a weigh-in part" — `testReviewWithoutAWeighInPart`; "Self-harm Yes then Yes at a restart" — `testSelfHarmYesThenYesAtARestart`; "Underweight at a re-screen" — `testUnderweightAtARescreen`; "No template yet" — `testNoTemplateYetWithTheLiveStage`; "Stage 1" — `testStage1NoMorningPlanReminder`; "An evening entry in stage 1" — `testAnEveningEntryInStage1`; "No entry by midday in stage 1" — `testNoEntryByMiddayInStage1`. Device checks (navigation depth, no automated test): "One tap to each screen", "The route from Today", "Weekly review" (Get support) — see the epic's device-check bead.

## 3. Accessibility (P2)

Read before the screen beads above, per CLAUDE.md.

- [x] 3.1 mm-t32.15 — weekly-review, "Accessibility of the review". Proves what 1.1–2.1 already built (CLAUDE.md: the P2 accessibility bead proves, it does not add). `ReviewAccessibilityTests`: "VoiceOver reads the summary" — each `ReviewSummary.parts` sentence is already the complete label text, `testEachSummarySentenceIsSelfContained`, plus `ReviewScreenView`'s own `.accessibilityElement(children: .ignore).accessibilityLabel(line)` per row (structural); "VoiceOver on the pinned note" — `TodayView`'s pinned note button carries the same pattern (structural; no App-target test runner); "VoiceOver on 'I'm getting worse'" — `testGettingWorseLabelAndHint`, plus the button's own `.accessibilityHint` (structural); "Largest text size" (the review half; `mm-t32b.5` proves the taking-stock half) — `testNoFixedWidthContent`, plus every field in `ReviewScreenView` uses system text styles (`.font` never set to a fixed size), structural. Deferred: `mm-t32b.5` builds "VoiceOver on the chosen module" and the taking-stock half of "Largest text size".
- [x] 3.2 mm-t32.14 — weekly-review, "What the review never shows". `ReviewNeverShowsTests`: "Fewer starred entries" — `testFewerStarredEntries`; "More starred entries" — `testMoreStarredEntries`; "Every planned meal with an entry" — `testEveryPlannedMealWithAnEntry`; "No planned meal with an entry" — `testNoPlannedMealWithAnEntry`. Also `testNoScoreRatingOrPercentage`.
- [x] 3.3 mm-t32.18 — safeguarding, "No question about vomiting or laxatives", scenario "Weekly review". `NoCompensationQuestionAtAWeeklyReviewTests.testWeeklyReviewQuestionsMentionNoCompensation`, running `ScreeningQuestionCatalog.mentionsCompensation(_:)` against every question the weekly review and the self-harm item show.

## 4. Device checks

- [ ] 4.1 lists every device-only check from the tasks above: "One tap to each screen" and "The route from Today" (mm-t32.16, navigation depth); "Weekly review" — Get support on the review and the Reviews list (mm-t32.16); "VoiceOver reads the summary", "VoiceOver on the pinned note", "VoiceOver on 'I'm getting worse'", "Largest text size" (mm-t32.15, the real VoiceOver walk and Dynamic Type layout). See the epic's device-check bead. Ash does the checks; per CLAUDE.md, a device-check bead does not gate the epic's closure.
